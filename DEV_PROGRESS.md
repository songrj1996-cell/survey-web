# Survey-Web 开发文档

> 文档用途：记录当前主干的产品能力、代码结构、运行方式、验证入口和近期变更。
>
> 当前分支：`main`
>
> 当前代码基线：`b3e9d6b`（2026-07-28）
>
> 远端状态：本地 `main` 比 `origin/main` 多 1 个提交（截至 2026-07-29）
>
> 最后更新：2026-08-03
>
> 项目路径：`C:\Users\admin\Desktop\survey-web`

---

## 一、项目概览

Survey-Web 是一个面向用户研究的调研分析工作台，当前包含四类主要能力：

| 模块 | 输入 | 核心处理 | 主要输出 |
|---|---|---|---|
| 定性分析 | 问卷回收表（CSV / XLSX / XLS） | 题型识别、方案规划、统计与开放题整理、按业务 Topic 生成报告 | 报告、追问、Word / PDF / Markdown / 飞书文档 |
| 定量分析 | 问卷、清数表、跑数表 | 读取已有统计结果、开放题聚类、章节规划 | 定量报告、追问及多格式导出 |
| 评论分析 | 评论或用户反馈表 | 大文件预处理、抽样、相关性筛选、主题与情感分析、原文精选 | 评论简报、历史记录及导出 |
| 数据标注 | 问卷回收表 | AI 作答识别、人工确认、逐题质量打标、统一中文翻译 | 带标注结果的 Excel、历史记录下载 |

定性与定量的区别以分析依据和报告目标为准：

- 定性分析从原始反馈和问卷数据中归纳发现，强调业务 Topic、玩家反馈证据和解释。
- 定量分析读取研究人员已经定义好的跑数结果，强调统计口径、交叉数据和量化结论。

系统还提供飞书登录、白名单与模块权限、管理员设置、审计日志、历史报告和报告导出。

---

## 二、当前代码架构

后端使用 FastAPI，前端使用原生 HTML / CSS / JavaScript。`server.py` 只保留兼容入口，应用装配位于 `app/main.py`。

```text
server.py
  └─ app/main.py
      ├─ routers/       HTTP 路由、参数校验、权限检查
      ├─ services/      业务流程编排
      ├─ storage/       data/ 下的本地持久化
      ├─ integrations/  公司 LLM 分发服务、飞书
      ├─ core/          配置、安全、解析、响应、审计等通用能力
      └─ schemas/       请求与响应数据结构

static/
  ├─ index.html
  ├─ style.css
  └─ js/
      ├─ core/core.js
      └─ features/
          ├─ survey.js
          ├─ report.js
          ├─ comment.js
          ├─ annotate.js
          ├─ history.js
          ├─ settings.js
          └─ nav.js
```

### 分层职责

| 层 | 主要职责 | 代表文件 |
|---|---|---|
| 路由层 | 接收请求、校验参数、检查权限、调用 service | `app/routers/survey.py`、`annotate.py`、`export.py` |
| 业务层 | 编排问卷、评论、标注、报告、导出流程 | `app/services/survey_service.py`、`report_engine.py`、`annotate_workflow.py` |
| 存储层 | 读写 session、历史、设置、白名单等本地数据 | `app/storage/sessions.py`、`history.py`、`whitelist.py` |
| 外部集成 | 封装外部服务协议与流式响应 | `app/integrations/llm_client.py`、`feishu_client.py` |
| 核心层 | 配置、安全、通用解析和响应格式 | `app/core/config.py`、`security.py`、`parsing.py` |

根目录的 `crosstab_parser.py`、`survey_plan.py`、`survey_stats.py`、`comment_analysis.py` 和 `annotate.py` 是现有算法或兼容模块，尚未全部移入 `app/`。

详细系统说明见：

- `harness-workbench/ARCHITECTURE.md`：系统全景、数据流和模块边界。
- `harness-workbench/RULES.md`：代码改动、数据保护、配置和验证规则。
- `docs/comment_analysis_llm.md`：评论分析七个直连模型任务及容错策略。
- `docs/annotate_llm.md`：数据标注三个直连模型任务、校验与补跑策略。
- `AGENTS.md`：Codex 项目级约束。
- `skills/survey-web-frontend-development/SKILL.md`：前端设计与交互规范。
- `skills/survey-analysis-workflow/SKILL.md`：调研分析业务流程规范。

---

## 三、核心业务流程

### 3.1 定性分析

```text
上传问卷回收表
  → AI 题型识别与人工确认
  → 补充业务背景、报告要求和可选行动建议
  → 生成或修订分析方案
  → Python 统计与原始反馈整理
  → 按业务 Topic 生成报告
  → 追问、历史归档和导出
```

当前要点：

- 支持矩阵题子项内联编辑。
- 支持问卷跳转逻辑推断与异常答题检查。
- 对选择题的明确 “Other/其他” 和无法匹配选项分别处理，避免统计口径混淆。
- 报告 Part 按业务 Topic 综合叙述，不再机械地按单题拆章。
- 玩家身份字段统一，便于在多章节中追踪同一受访者。
- 题型识别、方案规划、报告正文和追问均直连公司 LLM 分发服务。
- 追问前会提示本次回答使用的原始反馈范围：全部、分层抽样或无原始数据。
- 方案修订时，交叉分析画像列只能引用当前方案真实存在的画像维度。

### 3.2 定量分析

```text
上传问卷 + 清数表 + 跑数表
  → 自动建立列与题目映射
  → 生成并确认章节方案
  → 读取跑数表统计结果、整理开放题
  → 生成报告
  → 追问、历史归档和导出
```

当前原则：

- 已有统计数字由 Python 读取跑数表，不让 LLM 重新计算。
- LLM 负责理解问卷、组织章节、归纳开放题和撰写文字。
- 跑数表模式与定性分析使用独立的规划和报告配置。

### 3.3 评论分析

```text
上传评论文件
  → 流式预处理与清洗
  → 分层抽样
  → 主题相关性筛选及必要时动态补样
  → 主题提取、合并、分类和情感分析
  → 生成简报与精选原文
  → 历史归档和导出
```

当前边界：

- 面向大文件时 AI 不读取全部评论，而是先抽样和筛选。
- 报告中的比例属于分析样本口径，不应解释为严格的全量总体比例。
- 评论分析的七个模型任务均直连公司 LLM 分发服务，服务层负责结构校验、模型切换、重试和拆批兜底。

### 3.4 数据标注

```text
上传数据
  → 识别列、统一翻译表头并人工确认
  → AI 作答识别
  → 人工确认疑似结果
  → 逐题质量打标
  → 统一补齐非中文回答翻译
  → 下载标注结果并写入历史
```

当前要点：

- AI 识别、质量标签和中文翻译分别维护缺失结果集合，避免一个阶段的缺失误导另一个阶段。
- 三项任务使用独立的首选/备选模型；原两个 Dify 应用中的翻译分支已合并为统一翻译链。
- 对 `nil`、`N/A` 等明确占位回答做规范化处理，不把正常短回答一概当空值。
- 标注会话 `annotate_sessions` 仍为进程内状态，部署继续保持单 worker。
- 标注模式的“重新开始”只重置标注模块，不再误触发问卷分析会话重置。

---

## 四、AI 与外部服务边界

### 公司 LLM 分发服务

全部 AI 业务链路统一使用 `app/integrations/llm_client.py` 直连公司 LLM 分发服务，
包括题型识别、方案规划、大样本开放题、报告与追问、评论分析和数据标注。
各任务可独立配置首选模型、备选模型、推理强度、最大 token、超时、重试和并发；
服务层负责提示词组装、结构校验、补跑和业务状态。平台不再需要 Dify Key 或 Dify 客户端。

### 飞书

飞书集成负责 OAuth 登录和文档导出。报告导出支持自定义标题、去除正文重复一级标题，并将核心结论等重点内容转换为飞书高亮块。

所有密钥、外部服务地址和部署相关配置必须通过 `.env` 与 `app/core/config.py` 管理，不得写入代码或本文档。

---

## 五、数据与持久化

项目使用本地 JSON 和文件持久化，没有数据库。

| 路径 | 内容 |
|---|---|
| `data/sessions/` | 问卷、定量和评论分析的临时 session |
| `data/history.json` | 已完成报告与标注结果的历史索引 |
| `data/whitelist.json` | 用户及模块权限 |
| `data/prompts.json` | 管理端可调整的提示词 |
| `data/app_settings.json` | 平台开关 |
| `data/ui_texts.json` | 可配置界面文案 |
| `data/audit_logs.json` | 操作审计日志 |
| `data/web_logins.json` | 飞书登录状态 |
| `data/comment_uploads/` | 评论分析上传文件 |
| `data/annotate_results/` | 标注结果文件 |

`data/` 是真实运行数据。开发、测试和部署过程中不得删除、清空、覆盖或格式化其中任何内容。启动 FastAPI 应用可能触发过期 session 清理；需要端到端测试时，应先确认不会影响真实数据。

---

## 六、近期更新

### 2026-08-03

- 平台全部 AI 业务链路完成直连公司 LLM 分发服务迁移，移除 Dify 运行时依赖。
- 评论分析拆分为七个直连模型任务，并保留本地统计、重试、拆批和精选原文流程。
- 数据标注拆分为 AI 作答识别、逐题质量打标和统一中文翻译三项任务；合并两个旧应用重复的翻译分支。

### 2026-07-28 · `b3e9d6b`

- 追问增加原始反馈使用范围提示。
- 交叉分析方案修订限制为真实存在的画像维度。
- 修复标注模块“重新开始”误重置问卷会话。

### 2026-07-20 · `171d53a`

- 定性报告正文切换为直连 LLM 网关。
- 重构报告生成、追问上下文和前端报告交互。
- 增加模型回退、超时、重试和流式心跳配置。

### 2026-07-17 · `ae283d2`、`d4a19b1`

- 定性报告改为按业务 Topic 综合叙述，统一玩家身份字段。
- 增加问卷跳转推断与分支异常检测。
- 重构数据标注流程。
- 改进飞书导出高亮块、报告渲染、题型识别和上传格式处理。
- 新增对应的单元测试。

### 2026-07-10 至 2026-07-13 · `cc09034`、`a81cbc0`

- 定性报告支持补充业务背景和可选行动建议。
- 改进 Google Form “Other/其他”及未匹配选项的人工确认与统计流向。
- 矩阵题子项支持题型确认页内联编辑。

### 2026-07-03 · `b29b0c9`、`d01f33f`、`40c7c61`

- 飞书导出支持自定义标题并去除重复一级标题。
- 完成架构重构配套文档和 Python 验证约定。

---

## 七、本地开发

### 环境

- 使用 `python`（当前机器为 Python 3.14.3）或 `uv`。
- 不使用 `python3`、`conda` 或已废弃的 `.venv/`。
- 安装依赖时以 `requirements.txt` 为准，不擅自新增第三方包。

### 启动

```powershell
cd C:\Users\admin\Desktop\survey-web
python -m uvicorn server:app --host 0.0.0.0 --port 18081 --reload
```

注意：启动应用可能读取或清理 `data/sessions/` 中的过期文件。操作真实环境前先确认数据影响。

### 配置

1. 参考 `.env.example` 创建本地 `.env`。
2. 按实际启用的模块填写飞书和 LLM 分发服务配置。
3. 不提交 `.env`，不在日志、截图或文档中展示任何 Key、Cookie 或 token。

---

## 八、验证

### 后端边界检查

```powershell
python scripts/check_boundaries.py
```

### Python 语法检查

```powershell
python -m compileall app
```

### 单元测试

当前测试覆盖：

- 标注直连 LLM 与缺失结果处理
- 问卷跳转和分支异常
- Other / 未匹配选项
- LLM 客户端及各业务直连任务
- 报告结构与业务背景
- 飞书导出格式
- 上传格式

按改动范围运行对应测试，例如：

```powershell
python -m unittest tests.test_report_direct_llm
python -m unittest tests.test_annotate
```

运行可能导入应用或接触 `data/` 的测试前，必须先确认其副作用。不能安全执行时，至少进行静态检查、diff read-back 和 `git diff --check`，并明确说明未运行动态验证的原因。

前端改动还需要用浏览器对相关入口、加载态、错误态、返回路径和响应式布局做冒烟检查。

---

## 九、部署

仓库包含：

- `deploy.sh`：Ubuntu + systemd 部署，默认监听 `0.0.0.0:18081`。
- `nginx-survey.conf`：Nginx 配置。

部署脚本默认使用：

| 配置 | 值 |
|---|---|
| 部署目录 | `/opt/survey-web` |
| systemd 服务 | `survey-web` |
| 运行用户 | `www-data` |
| 端口 | `18081` |

部署要求：

- `data/` 和 `.env` 不从开发目录覆盖到部署目录。
- 数据标注会话仍依赖进程内状态，当前必须使用单 worker。
- 本文档不再记录未经服务器核验的“当前云上提交”。部署前应在服务器上分别确认实际 commit、环境变量完整性、worker 数量和数据目录。
- 修改 `deploy.sh`、`nginx-survey.conf`、`.env.example`、`app/main.py`、`app/core/config.py` 或外部集成文件前，需要单独说明部署影响。

---

## 十、当前限制与后续关注

| 项目 | 当前状态与影响 |
|---|---|
| 本地文件持久化 | 适合单机和小团队；高并发、多实例和复杂查询能力有限 |
| 标注会话 | `annotate_sessions` 为进程内状态，多 worker 会造成会话不可见 |
| 评论分析抽样 | 报告比例是分析样本口径，不是严格全量总体占比 |
| LLM 结构化输出完整性 | 大批量分类依赖完整索引返回，失败会增加模型切换、重试、拆批和总耗时 |
| 外部服务依赖 | LLM 分发服务或飞书不可用时，对应分析、报告或登录/导出能力会受影响 |
| 线上版本 | 仓库无法证明服务器当前运行的 commit，发布前必须现场核验 |

---

## 十一、Git 状态与文档维护

截至 2026-07-29：

- 本地 `main`：`b3e9d6b`
- `origin/main`：`171d53a`
- 本地比远端多 1 个提交

本文档应在以下情况同步更新：

- 新增或移除用户可见模块。
- 业务流程、AI 调用边界或数据口径变化。
- 架构目录、启动命令、验证命令或部署方式变化。
- 已知限制被修复，或新增会影响开发/部署的限制。

提交、推送和部署状态只记录可通过 Git 或目标服务器核验的事实，避免保留推测性版本信息。
