# survey-web 项目规则

## 必读文件

开始任何任务前，必须先阅读并遵守：

- `harness-workbench/RULES.md`
- `harness-workbench/ARCHITECTURE.md`

`RULES.md` 是本项目的行为准则，`ARCHITECTURE.md` 是理解系统运行方式和模块边界的参考。

---

## 最关键的规则

### 1. 改动前必须确认

新增、编辑、删除或重命名任何文件前，必须先告诉用户；代码、测试、文档、配置、Skill 和临时脚本均适用：

1. 打算改哪个文件（精确到路径）
2. 具体改什么；涉及 bug 时，列明要修复的问题
3. 为什么要改
4. 预期达成的效果
5. 对报告质量和报告生成速度分别可能产生什么负面影响，包括触发条件、程度、发生情况和判断依据
6. 改完后怎么验证这些效果及风险

只有文件和具体修改或 bug 修复均得到用户明确同意后，才能开始。批准修改某个文件，不代表批准修复该文件中的其他 bug。

同一方案的明确批准在当前任务及该范围内持续有效，不因换轮、逐个修改已批准文件或执行已批准验证而重复确认。不得从“继续”“完成任务”等表述中推导出对新文件或新 bug 的授权。

负面影响按“必然发生”“特定条件下必然发生”“可能发生”“暂未发现直接影响”或“证据不足，无法判断”说明，具体要求见 `RULES.md` 第一节。没有证据不得编造概率百分比，也不得把未验证表述为没有影响。

### 2. 发现问题只报告，不擅自修改

发现未经用户确认要修复的 bug 或其他额外问题、风险、优化点，只能报告，不能顺手修改。即使问题位于已批准文件内，或被认为是完成目标的必要条件，也必须先说明并取得针对该问题及涉及文件的批准。

需要新增授权时，仅暂停依赖它的工作，继续其他独立且已授权的事项。

### 3. 改动范围严格等于用户要求

用户要求改 A，就只改 A。未经要求的任何改动，一律不做。

### 4. 保护 data/ 目录

`data/` 下存放运行时真实数据。未经针对具体数据操作的单独明确批准，不得删除、清空、覆盖或格式化其中任何文件和目录；获准的数据操作仍须满足 `harness-workbench/RULES.md` 第六节的备份、恢复和校验条件。代码修改、测试、合并或普通工作树清理的批准均不包含真实数据操作。

### 5. 密钥和配置

不得在代码中硬编码 API Key、外部服务地址、部署路径或敏感配置。配置统一走 `.env` 和 `app/core/config.py`。

### 6. 验证和说明

文件改动完成后，必须说明做了哪些验证，并用实际结果更新对报告质量和生成速度负面影响的判断；如果没有运行验证，必须说明原因和仍无法判断的影响。

### 7. 全项目任务分类与隔离开发约定

本约定适用于整个 survey-web 项目的所有当前及未来任务、分支和工作树。用户不需要使用
固定触发语；应根据需求语义主动判断是 BUG、新功能、优化还是混合任务，并进入
`harness-workbench/RULES.md` 第十五节规定的分类、短期分支和独立工作树流程。

用户只要求诊断、解释或报告时保持只读，不创建分支或修改文件。用户明确要求修改、实现或
修复，且分类清晰时，应先报告分类、建议分支、基线、工作树和当前状态，再准备对应环境；
分类会实质影响方案且无法可靠判断时，先只读核查并请用户决定。

任务名称前缀必须随真实进度更新；状态名称只报告当前进度，既不新增授权，也不撤销已有授权。
已批准的工作和验证应继续执行；文件修改前、合入 `main` 前及数据操作前原本需要的用户审核和明确确认仍适用。

---

## Python 环境与验证

本机可用的 Python 环境：

- `python`：可用，当前为 Python 3.14.3。
- `uv`：可用。

不要使用：

- `python3`
- `conda`
- `.venv/`：该虚拟环境已废弃，指向的 Python 3.10 已不存在。

验证约定：

- 后端边界检查命令固定为：`python scripts/check_boundaries.py`
- Python 语法检查优先使用：`python -m compileall app`
- 如果 Codex 沙箱里 `python` 无法运行，不要反复尝试 `.venv`、`python3`、`conda` 或下载新的解释器；改为做静态检查（例如 diff/read-back/语法层面的人工检查），并在最终说明中明确写出“未运行 Python 验证”的原因。
